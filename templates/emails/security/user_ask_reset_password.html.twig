<!DOCTYPE html>
<html>
    <head>
        <title>
            {{ presenter.email.title|trans }}
        </title>
    </head>
    <body>
        <section>
            <h2>
                {{ presenter.email.header|trans }}
            </h2>
            <div>
                {{ presenter.email.content|trans }}
                <a href="{{ url('web_reset_password', {"token": user.resetPasswordToken}) }}">
                    {{ 'user.ask_reset_password.text'|trans }}
                </a>
            </div>
        </section>
    </body>
</html>
