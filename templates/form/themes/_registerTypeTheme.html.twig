{% block form %}
    <div class="mdc-card">
        <div class="mdc-card__media mdc-card__media--square">
            <div class="mdc-card__media-content">Title</div>
        </div>
        <!-- ... content ... -->
        <div class="mdc-card__actions">
            <div class="mdc-card__action-buttons">
                <button class="mdc-button mdc-card__action mdc-card__action--button">Action 1</button>
                <button class="mdc-button mdc-card__action mdc-card__action--button">Action 2</button>
            </div>
            <div class="mdc-card__action-icons">
                <i class="material-icons mdc-card__action mdc-card__action--icon" tabindex="0" role="button" title="Share">share</i>
                <i class="material-icons mdc-card__action mdc-card__action--icon" tabindex="0" role="button" title="More options">more_vert</i>
            </div>
        </div>
    </div>
    <div class="mdl-grid contact-form">
        <div class="mdl-cell mdl-cell--12-col mdl-card mdl-shadow--4dp">
            <div class="mdl-card__title">
                <h2 class="mdl-card__title-text">
                    {{ "account.creation"|trans }}
                </h2>
            </div>
            <div class="mdl-card__media">
            </div>
            <div class="mdl-card__supporting-text">
                {{ form_start(form) }}
                {{ form_errors(form) }}

                {{- form_row(form.username) -}}
                {{- form_row(form.email) -}}
                {{- form_row(form.password) -}}

                <div class="mdl-textfield mdl-js-textfield mdl-textfield--file">
                    <input class="mdc-text-field__input" placeholder="{{ "Profile image"|trans }}" type="text" id="uploadFile" readonly/>
                    <div class="mdl-button mdl-button--primary mdl-button--icon mdl-button--file">
                        <i class="material-icons">attach_file</i>
                        {{- form_row(form.profileImage) -}}
                    </div>
                </div>
                <div>
                    <button class="mdc-button mdc-button--raised mdc-elevation--z4">
                        {{ "account.creation"|trans }}
                    </button>
                </div>
                {{ form_end(form) }}
                <p class="registration-bloc__login-text">
                    <a href="{{ path('web_login') }}">{{ "account.already_exist"|trans }}</a>
                </p>
            </div>
        </div>
    </div>
{% endblock %}

{% block form_row %}
    <div class="mdc-text-field mdc-text-field--fullwidth">
        {{ form_widget(form, {'attr': {'class': 'mdc-text-field__input'}}) }}
        {{ form_errors(form, {'attr': {'class': 'mdl-textfield__error'}}) }}
    </div>
{% endblock %}
